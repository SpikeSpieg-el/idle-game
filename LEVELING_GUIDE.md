# 📖 Гайд по системе прокачки персонажей

## 🎯 Основные изменения

### ✨ Все характеристики теперь полезны!

Каждая характеристика влияет на геймплей:

| Характеристика | Иконка | Влияние на геймплей |
|----------------|--------|---------------------|
| **Сбор (harvest)** | 🌾 | Увеличивает добычу дерева, еды, трав, гемов в экспедициях |
| **Удача (luck)** | 🍀 | Шанс критической добычи (+50% от вклада), бонусный опыт, влияет на травы/шкуры/артефакты/гемы |
| **Ремонт (repair)** | 🔧 | Увеличивает добычу камня и железной руды |
| **Магия (magic)** | ✨ | Увеличивает добычу эссенции и древних артефактов |
| **Скорость крафта (craft_speed)** | ⚒️ | Ускоряет крафт в мастерской (макс. -80% времени при 200 очках) |
| **Алхимия (alchemy_speed)** | 🧪 | Ускоряет алхимические рецепты (макс. -80% времени при 200 очках) |
| **Мораль (morale)** | 💪 | Снижает расход еды в экспедициях (до -40% при 100 очках) |
| **Защита (defense)** | 🛡️ | Снижает риск потерь в опасных местах, важна для охоты |
| **Чутьё (rare_find)** | 🔍 | Шанс найти бонусные ресурсы (до +30% при 100 очках) |
| **Кулинария (food)** | 🍲 | Увеличивает выход готовой еды (до +100% при 100 очках) |
| **Сила (strength)** | 💥 | Увеличивает добычу железной руды и шкур зверей |

## 🔄 Динамическая стоимость прокачки

### 1. Вливание в стат (Stat Infusion)
- **Базовая стоимость**: 1 очко навыка
- **Динамика**: Каждые 20 очков стата увеличивают стоимость на +1
  - 0-19 очков стата → 1 SP за улучшение
  - 20-39 очков → 2 SP за улучшение
  - 40-59 очков → 3 SP за улучшение
  - и так далее...

### 2. Улучшение ранга (Improve Rank)
- **Формула**: 5 + (текущий_ранг × 3)
- **Эффект**: +5% ко всем разблокированным статам
  - 1-й ранг → 5 очков
  - 2-й ранг → 8 очков
  - 3-й ранг → 11 очков

### 3. Разблокировка нового стата (Unlock Extra Stat)
- **Формула**: 8 + (количество_разблокированных × 2)
- **Эффект**: Дает стартовый бонус 5 + уровень персонажа
  - 1-й доп стат → 8 очков
  - 2-й доп стат → 10 очков
  - 3-й доп стат → 12 очков

### 4. Повышение звездного уровня (Increase Star Level)
- **Формула**: 12 + (текущие_звезды × 4)
- **Эффект**: +2 ко всем разблокированным статам
  - 1-я звезда → 12 очков
  - 2-я звезда → 16 очков
  - 3-я звезда → 20 очков

### 5. Повышение редкости (Upgrade Rarity)
- **Формула**: 25 + (текущая_редкость × 10)
- **Максимум**: 6★
- **Эффект**: 
  - +(3 + текущая_редкость) ко всем разблокированным статам
  - +(текущая_редкость + 1) очков навыков в подарок
  - 3★ → 4★: 55 очков → +6 ко всем статам + 4 SP
  - 4★ → 5★: 65 очков → +7 ко всем статам + 5 SP
  - 5★ → 6★: 75 очков → +8 ко всем статам + 6 SP

## 🌟 Специализации (новое!)

**Стоимость**: 20 очков навыка
**Ограничение**: Выбирается только ОДИН РАЗ навсегда!

### Доступные специализации:

1. **Собиратель (Gatherer)** 🌾
   - +15 к Сбору
   - +10 к Удаче
   - Разблокирует оба стата

2. **Мастер (Crafter)** ⚒️
   - +20 к Скорости крафта
   - +10 к Ремонту
   - Разблокирует оба стата

3. **Алхимик (Alchemist)** 🧪
   - +20 к Алхимии
   - +10 к Магии
   - Разблокирует оба стата

4. **Воин (Warrior)** ⚔️
   - +15 к Силе
   - +15 к Защите
   - Разблокирует оба стата

5. **Поддержка (Support)** 💝
   - +15 к Морали
   - +15 к Кулинарии
   - Разблокирует оба стата

## 💎 Конвертация гемов (новое!)

**Формула**: 1 гем = 3 очка навыка

Гемы можно потратить на прокачку вместо ожидания получения опыта!

**Как использовать** (требуется интеграция в UI):
```javascript
// В модалке прокачки добавить кнопку
const result = Leveling.convertGemsToSkillPoints(girl, gemsAmount, currentGemsResource);
if (result.success) {
  // Обновить ресурсы
  gameState.resources.gems -= result.gemsUsed;
  // Обновить UI
}
```

## 🔗 Синергии и пассивные бонусы

Система автоматически вычисляет пассивные бонусы:

### Критический шанс
- **Источник**: Удача
- **Формула**: min(50%, luck × 0.5%)
- **Применение**: Шанс +50% к личной добыче при завершении экспедиции

### Снижение расхода еды
- **Источник**: Мораль
- **Формула**: min(40%, morale × 0.4%)
- **Применение**: Снижает расход еды в экспедициях

### Снижение урона
- **Источник**: Защита
- **Формула**: min(50%, defense × 0.5%)
- **Применение**: Защита в опасных локациях

### Бонус к готовой еде
- **Источник**: Кулинария
- **Формула**: min(100%, food × 1.0%)
- **Применение**: Увеличивает выход при крафте готовой еды

### Шанс бонусных ресурсов
- **Источник**: Чутьё
- **Формула**: min(30%, rare_find × 0.3%)
- **Применение**: Шанс найти дополнительные ресурсы

### Автоматическая синергия
- **Условие**: Сбор > 30 И Удача > 30
- **Эффект**: +⌊(harvest + luck) / 20⌋ к Чутью

## 📊 Рекомендации по прокачке

### Для собирателей ресурсов:
1. Сначала возьми специализацию **Собиратель**
2. Прокачивай **Сбор** и **Удачу** до 50+
3. Разблокируй **Чутьё** для синергии
4. Вкладывай в **Мораль** для снижения расхода еды

### Для крафтеров:
1. Специализация **Мастер**
2. Макси **Скорость крафта** до 200 (макс ускорение -80%)
3. Добавь **Ремонт** для добычи камня/руды
4. **Кулинария** для производства еды

### Для магов/алхимиков:
1. Специализация **Алхимик**
2. Прокачай **Алхимию** до 200
3. Высокая **Магия** для эссенции и артефактов
4. **Чутьё** для редких находок

### Для воинов/охотников:
1. Специализация **Воин**
2. **Сила** для добычи шкур и руды
3. **Защита** для выживания
4. **Удача** для критов

## 🎮 Стратегии оптимизации

### Ранняя игра (уровень 0-10):
- Фокус на 2-3 основных статах
- Не торопись с разблокировкой новых статов
- Копи на специализацию (20 очков)

### Средняя игра (уровень 10-30):
- Выбери специализацию
- Доведи основные статы до 50-100
- Начни разблокировать синергичные статы

### Поздняя игра (уровень 30+):
- Прокачай ключевые статы до 200 (для крафта/алхимии)
- Повышай редкость для мощного буста
- Используй гемы для быстрой прокачки
- Собирай пассивные бонусы

## 🔧 Интеграция в UI (для разработчика)

### Добавить кнопку конвертации гемов:
```javascript
// В модалке прокачки (openLevelModal)
const gemsCount = gameState.resources.gems || 0;
const convertButton = `
  <button onclick="convertGems('${girlId}')" ${gemsCount < 1 ? 'disabled' : ''}>
    💎 Обменять гемы (1 гем = 3 SP) [${gemsCount} доступно]
  </button>
`;

function convertGems(girlId) {
  const girl = gameState.ownedGirls.find(g => g.ID === girlId);
  const gems = parseInt(prompt('Сколько гемов обменять? (1 гем = 3 SP)', '1'));
  
  if (!gems || gems < 1) return;
  
  const result = Leveling.convertGemsToSkillPoints(girl, gems, gameState.resources.gems);
  
  if (result.success) {
    gameState.resources.gems -= result.gemsUsed;
    updateResourcesUI();
    showCustomAlert(`Получено ${result.pointsGained} очков навыка!`);
    openLevelModal(girlId); // Обновить модалку
  } else {
    showCustomAlert(result.message);
  }
}
```

### Отображение пассивных бонусов:
```javascript
// Показывать passiveBonuses в модалке деталей персонажа
const bonuses = girl.passiveBonuses || {};
let bonusHTML = '<h4>Пассивные бонусы:</h4><ul>';
if (bonuses.critChance) bonusHTML += `<li>🎯 Крит шанс: ${bonuses.critChance.toFixed(1)}%</li>`;
if (bonuses.foodCostReduction) bonusHTML += `<li>🍲 Экономия еды: ${bonuses.foodCostReduction.toFixed(1)}%</li>`;
if (bonuses.damageReduction) bonusHTML += `<li>🛡️ Снижение урона: ${bonuses.damageReduction.toFixed(1)}%</li>`;
if (bonuses.foodBonus) bonusHTML += `<li>👨‍🍳 Бонус к еде: +${bonuses.foodBonus.toFixed(0)}%</li>`;
if (bonuses.bonusResourceChance) bonusHTML += `<li>💰 Шанс бонусов: ${bonuses.bonusResourceChance.toFixed(1)}%</li>`;
bonusHTML += '</ul>';
```

## 📈 Формулы влияния на геймплей

### Крафт/Алхимия:
```
Редукция_времени = min(0.8, craft_speed * 0.004)
Финальное_время = baseTime × (1 - редукция_статов) × (1 - редукция_уровня)
```

### Экспедиции:
```
Добыча = девушки × baseGain × multiplier_дальности
Бонус_от_стата = ⌊сумма_статов / делитель⌋ × multiplier
```

---

**Важно**: После обновления системы все девушки сохраняют свои текущие статы. Новые поля (specialization, passiveBonuses) инициализируются автоматически.
